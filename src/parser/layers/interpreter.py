from typing import Optional, Dict


class NLPLayer:
    def parse(self, text: str) -> Optional[Dict]:
        # Ici on mettra du NLP léger (spaCy, embeddings, etc.)
        # Pour l'instant, on fait juste un placeholder
        return None
